<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container py-4">
  <h1><%= emp.name %></h1>
  <ul class="list-group">
    <li class="list-group-item"><strong>Email:</strong> <%= emp.email %></li>
    <li class="list-group-item"><strong>Age:</strong> <%= emp.age || "N/A" %></li>
  </ul>
  <div class="mt-3">
    <a href="/employees/" class="btn btn-warning">Confirm </a>
    <a href="/employees/<%=emp._id %>/edit" class="btn btn-secondary">Cancel</a>
    <form action="/employees/<%= emp._id %>?_method=DELETE" method="post" style="display:inline;">
      <button class="btn btn-danger" onclick="return confirm('Delete this employee?')">Delete</button>
    </form>
    

  </div>
</body>
</html>
